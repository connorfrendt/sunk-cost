// Central place for magic numbers so tuning your game doesn't mean hunting through scenes.
export const GAME_WIDTH = 960;
export const GAME_HEIGHT = 540;

export const FONT = {
  family: 'monospace',
  color: '#ffffff',
  dim: '#aaaaaa',
  accent: '#4ade80',
};

export const PLAYER = {
  speed: 200,
};
